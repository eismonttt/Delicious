<Window x:Class="Delicious.Page1Admin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Delicious"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1Admin">

    <Grid>
        <DataGrid x:Name="restGrid"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="qwe" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="qwe" Binding="{Binding Location}"/>
                <DataGridTextColumn Header="qwe" Binding="{Binding Image}"/>
                <DataGridTextColumn Header="qwe" Binding="{Binding OpensTime}"/>
                <DataGridTextColumn Header="qwe" Binding="{Binding ClosesTime}"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal"
                    Width="120"
                    Height="30"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Center"
                    >
            <Button Content="Отмена" Click="OnCancel"/>
            <Button Content="Сохранить" 
                    Margin="10 0 10 0" 
                    Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
