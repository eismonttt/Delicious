<Window x:Class="Delicious.Page1Admin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Delicious"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1Admin">

    <Grid>
        <DataGrid x:Name="restGrid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="qwe" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="qwe" Binding="{Binding Location}"/>
                <DataGridTextColumn Header="qwe" Binding="{Binding Image}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
